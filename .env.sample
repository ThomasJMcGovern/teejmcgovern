# TJ.AI chat widget — OpenAI
# Copy to .env.local and fill in. Also add OPENAI_API_KEY to the Vercel project
# env (Settings -> Environment Variables) so the deployed site answers too.
OPENAI_API_KEY=sk-...
# Optional — defaults to gpt-4.1-mini
# OPENAI_MODEL=gpt-4.1-mini
